import React from "react"
import { IState as IProps } from "./App"

const List: React.FC<IProps> = ({ form }) => {
    return (
        <div>
            List
        </div>
    )
}

export default List